$(document).ready(function(){

 // $('.single-item').slick();

  $('.multiple-items').slick({
    infinite: false,
    dots: true,
    slidesToShow: 1,
    slidesToScroll: 1
  });

 /*  $(".multiple-items").slick({
    infinite: true,
    slidesToShow: 3,
    slidesToScroll: 3,
  }); */
});
